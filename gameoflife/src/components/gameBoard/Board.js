import React,{useState,useEffect} from 'react';
import './board.css'

const Board = (props) => {
    const cellSize = 20;
    const WIDTH = 800;
    const HEIGHT = 600;
    return ( <div className = 'Board'  style={{ width: WIDTH, height: HEIGHT, backgroundSize: `${cellSize}px ${cellSize}px` }}>asdfadsf</div> );
}
 
export default Board;